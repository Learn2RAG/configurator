services:
  ollama:
    command: ["{{ollama_bin}}", "serve"]
    environment:
      OLLAMA_HOST: "http://127.0.0.1:{{port}}"
      OLLAMA_MODELS: "{{storage_path}}/ollama/models"
      OLLAMA_NOHISTORY: "true"
      OLLAMA_TMPDIR: "{{storage_path}}/ollama/tmp"
